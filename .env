# Server
PORT=3000

# Number of parallel ARGON2ID workers (default: 4)
# Set to match your CPU cores for best performance
WORKERS=4

# Optional: Proxy URL for requests (leave empty for direct)
# PROXY_URL=http://user:pass@proxy:80
